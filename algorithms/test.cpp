#include <bits/stdc++.h>

using namespace std;

int aa()
	{
		int cloc = clock();
		//do something that takes a few milliseconds
		for ( int i = 0 ; i < 100000 ; i++)
			{
				int x = 2;
			}
		cout << (clock() - cloc) << endl;
	}